{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "installCommand": "npm install --package-lock=false --no-audit --no-fund --registry=https://registry.npmjs.org/",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
